name: dl-project
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch
  - torchvision
  - torchaudio
  - cpuonly
  # Add other conda packages here
  - pip:
      - numpy
      - pandas
      - matplotlib
      - scikit-learn
      - pyyaml
      - tensorboard
      - tqdm
      - opencv-python
      - thop
      - gradio>=6.0
      - imageio
      - imageio-ffmpeg
